# Shared link-checker defaults for CI (`.github/workflows/docs-links.yml`) and local runs.
# Keeps fragile settings out of shell `eval` in lychee-action.
#
# TOML literal strings (single quotes) are not backslash-escaped. Use `\.` so the
# regex matches a literal dot. `\\.` looks for a backslash and never matches.

exclude = [
  '^https://github\.com/techuties/tu-vm/compare/',
  '^https://github\.com/user-attachments/',
  # AFFiNE docs CDN/WAF returns 403 to CI user-agents; the human URL remains in README.md.
  '^https://docs\.affine\.pro',
]

max_retries = 6
timeout = 30
retry_wait_time = 2

user_agent = "Mozilla/5.0 (compatible; TU-VM-docs-links/1.0; +https://github.com/techuties/tu-vm)"
